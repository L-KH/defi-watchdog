// Simple Navigation Fix\n// Add this script to force navigation for audit pages\n\nif (typeof window !== 'undefined') {\n  // Fix navigation between audit pages\n  window.fixNavigation = () => {\n    const navLinks = document.querySelectorAll('a[href=\"/audit\"], a[href=\"/audit-pro\"]');\n    \n    navLinks.forEach(link => {\n      link.addEventListener('click', function(e) {\n        e.preventDefault();\n        const targetPath = this.getAttribute('href');\n        \n        console.log('🔗 Fixed navigation to:', targetPath);\n        \n        // Force page reload for audit navigation\n        window.location.href = targetPath;\n      });\n    });\n  };\n  \n  // Auto-fix on page load\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', window.fixNavigation);\n  } else {\n    window.fixNavigation();\n  }\n}\n\nexport default function NavigationFix() {\n  return null;\n}