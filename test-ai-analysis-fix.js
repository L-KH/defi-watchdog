// Quick test to verify the fix\nconsole.log('Testing REPORT_GENERATION_PROMPTS definition...');\n\n// Test the imports and function\ntry {\n  // Import the main function\n  const { analyzeWithAI } = require('./aiAnalysis');\n  \n  // Test that the function exists\n  if (typeof analyzeWithAI === 'function') {\n    console.log('✅ analyzeWithAI function imported successfully');\n  } else {\n    console.log('❌ analyzeWithAI is not a function');\n  }\n  \n  console.log('✅ All imports successful - no ReferenceError');\n  console.log('🎉 Full scan should now work without errors!');\n  \n} catch (error) {\n  console.error('❌ Error testing the fix:', error.message);\n}\n